## [release/0.2.0] - 2026-08-03

### 🚀 Features

- *(web)* Optimize store after handling data post (#202)
- Add string encoding to the set of supported stroage encodings (#209)
- Implement transaction pattern for storage layers (#207)
- Allow settings a maximum time for which the operations log can be stale (#218)
- Avoid spawning additional runtime by using the current runtime (#222)
- Add `RdfFusionConfig` (#226)
- Add basic support for directly querying data dumps (#232)
- Implement generating data dumps with rdf-fusion (#239)
- Add support for dumping stores as parquet (#245)
- Support querying parquet files with RDF data (#246)
- *(bench)* Add benchmarking support for parquet storage (#249)
- Minor improvement to the cli (#250)
- Improve large-scale data ingestions and parquet querying (#252)
- Add basic graph pattern node and naïve join ordering heuristic (#254)
- *(parquet)* Apply pruning early to set the statistics based on the surviving row count (#259)
- *(bench)* Allow concurrent queries in rdf-fusion-bench (#260)
- *(parquet)* Enable bloom filters (#262)
- *(parquet)* Custom scan execution plan operator that prevents pushing down complex filters (#268)
- *(parquet)* Allow bloom filter cache to fulfill partial get_ranges requests (#273)
- *(delta)* Only wrap the index scan in a custom query plan during scanning a delta table (#280)
- *(delta)* Explicitly materializes rdf terms from object ids (#279)
- *(delta)* Improve planning for exists queries to delay decoding (#283)
- *(delta)* Apply eager pruning and use result for join ordering (#285)
- *(delta)* Allow larger-than memory object id dictionaries by using btrees (#286)
- *(delta)* Make local object id store support transaction and integrate it with delta transactions (#289)
- *(delta)* Further try to improve performance and stability for large transactions (#292)
- *(parquet)* Allow caching data pages in our parquet scan plans (#296)
- *(debug)* Allow attaching tokio console for debuggin (#297)
- Various improvements (#302)

### 🐛 Bug Fixes

- Fix issues in tests and documentation (#238)
- *(parquet)* Improve bloom filter space efficiency and add tests (#264)
- Compiler error in benchmark (#271)
- *(tests)* Fix bgp planner tests, lints, and add more ci workflows (#272)
- *(delta)* Fix various issues that made problems with large transactions (#291)
- *(expr)* Remove async class realized via blocking calls in sync context (#294)
- Case in release process

### 📚 Documentation

- Update examples in readme (#278)

### 🧪 Testing

- *(parquet)* Add test for parquet pruning efficiency (#265)

### ⚙️ Miscellaneous Tasks

- Remove the datafusion patch as delta-rs now depends on datafusion 53.1 (#204)
- Formatting (#263)
- Remove naïve zorder implementation (#276)
- *(bench)* Remove wind farm benchmark and improve ci benchmark running (#288)
- *(delta)* Align delta quads implementation with suggested conventions (#290)
- Small refactorings before 0.2.0 (#305)
- *(release)* Improve release process with the help of the ci (#304)
