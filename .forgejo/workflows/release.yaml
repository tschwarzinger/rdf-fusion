name: Release

on:
  push:
    tags:
      - 'release/*'

jobs:
  validate:
    name: Validate Version
    runs-on: tuw-runner
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: https://github.com/dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97
      - name: Rust Cache
        uses: https://github.com/Swatinem/rust-cache@v2
      - name: Install Just
        run: cargo install just
      - name: Check Version
        run: just ci-check-version ${{ github.ref_name }}

  publish-crates:
    name: Publish to Crates.io
    needs: validate
    runs-on: tuw-runner
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: https://github.com/dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97
      - name: Rust Cache
        uses: https://github.com/Swatinem/rust-cache@v2
      - name: Install Just
        run: cargo install just
      - name: Publish
        run: just ci-publish-crates
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  build-binaries:
    name: Build Binaries
    needs: validate
    runs-on: tuw-runner
    strategy:
      matrix:
        target: [ x86_64-unknown-linux-gnu ]
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: https://github.com/dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97
          targets: ${{ matrix.target }}
      - name: Rust Cache
        uses: https://github.com/Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
      - name: Install Just
        run: cargo install just
      - name: Build Binary
        run: just ci-build-binary ${{ matrix.target }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.target }}
          path: target/rdf-fusion-*.tar.gz

  build-source:
    name: Build Source Tarball
    needs: validate
    runs-on: tuw-runner
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: https://github.com/dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97
      - name: Rust Cache
        uses: https://github.com/Swatinem/rust-cache@v2
      - name: Install Just
        run: cargo install just
      - name: Package Source
        run: just ci-package-source
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: source-tarball
          path: target/rdf-fusion-source.tar.gz

  create-release:
    name: Create Forgejo Release
    needs: [ build-binaries, build-source ]
    runs-on: tuw-runner
    steps:
      - uses: actions/checkout@v4
      - name: Download Binaries
        uses: actions/download-artifact@v4
        with:
          pattern: binary-*
          path: artifacts
          merge-multiple: true
      - name: Download Source
        uses: actions/download-artifact@v4
        with:
          name: source-tarball
          path: artifacts
      - name: Create Release
        uses: https://github.com/softprops/action-gh-release@v2
        with:
          files: artifacts/*
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}